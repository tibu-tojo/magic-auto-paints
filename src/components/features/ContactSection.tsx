import { Mail, MapPin, Phone } from "lucide-react";

import { ConsentMap } from "@/components/contact/ConsentMap";
import { ContactForm } from "@/components/contact/ContactForm";
import { SectionHeading } from "@/components/ui/SectionHeading";
import { business } from "@/lib/site";

export function ContactSection(){return <section id="contact" className="section border-t border-white/10 bg-[#0a0a0a]"><div className="shell"><div className="reveal"><SectionHeading eyebrow="Contact us" title="Tell us what needs attention." description="Send a few details and, if helpful, email photos or video. We’ll explain the next step and arrange a free quotation." /></div><div className="mt-12 grid gap-10 lg:grid-cols-[1.15fr_.85fr]"><div className="reveal"><ContactForm/></div><aside className="reveal space-y-5" style={{"--delay":"100ms"} as React.CSSProperties}><div className="surface p-7"><p className="eyebrow">Contact information</p><div className="mt-5 grid gap-4"><a className="contact-link text-lg" href={`tel:${business.officeHref}`}><Phone className="text-orange-500"/> Office: {business.officeDisplay}</a><a className="contact-link text-lg" href={`tel:${business.mobileHref}`}><Phone className="text-orange-500"/> Mobile: {business.mobileDisplay}</a><a className="contact-link break-all" href={`mailto:${business.email}`}><Mail className="text-orange-500"/> {business.email}</a></div></div><div className="surface p-7"><p className="eyebrow">Business location</p><div className="flex items-start gap-4"><MapPin className="mt-1 shrink-0 text-orange-500"/><address className="not-italic leading-7 text-white/60"><strong className="text-white">Magic Auto Paint</strong><br/>{business.addressLines.map(line=><span key={line}>{line}<br/></span>)}</address></div><div className="mt-5 border-t border-white/10 pt-5"><p className="text-xs font-bold uppercase tracking-[.15em] text-white/70">Opening hours</p><p className="mt-2 text-sm leading-6 text-white/45">We are currently working temporary hours while completing a contract. Email photos for quotations or call to arrange an evening or weekend appointment.</p></div></div></aside></div><div className="mt-14"><p className="eyebrow">How to find us</p><h3 className="display-title mb-4 text-4xl sm:text-5xl">Dorking West Station Yard</h3><p className="mb-8 max-w-3xl text-sm leading-7 text-white/50">Drive past LJC Auto Spares Ltd and follow the road for around 250 yards. The Magic Auto Paint sign will be on the fence to your left.</p><div className="surface overflow-hidden"><ConsentMap/></div></div></div></section>}
